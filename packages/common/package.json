{
  "name": "@vc/common",
  "version": "0.1.0",
  "main": "src/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "test": "npm run build"
  },
  "devDependencies": {
    "typescript": "^4.4.3"
  }
}
